# portal-api

Backend del portal de clientes.

> Nota: el backend actual vive en `backend/`. Esta carpeta es el destino objetivo para migración gradual.

## Endpoints objetivo
- `GET /health`
- `GET /me`
- `GET /portal/projects`
- `GET /portal/schedules`
- `POST /portal/invites`
- `POST /portal/invites/accept`
